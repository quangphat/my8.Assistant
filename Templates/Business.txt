using my8.Api.IBusiness;
using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;
using MongoI = my8.Api.Interfaces.Mongo;
using NeoI = my8.Api.Interfaces.Neo4j;
using my8.Api.Models;
using AutoMapper;
namespace my8.Api.Business
{
    public class {ModelName}Business : I{ModelName}Business
    {
        MongoI.I{ModelName}Repository m_{ModelName}RepositoryM;
        public {ModelName}Business(MongoI.I{ModelName}Repository {modelname}RepoM)
        {
            m_{ModelName}RepositoryM = {modelname}RepoM;
        }
        public async Task<{ModelName}> Create({ModelName} {modelname})
        {
            string id = await m_{ModelName}RepositoryM.Create({modelname});
            {modelname}.Id = id;
            return {modelname};
        }

        public async Task<{ModelName}> Get(string {modelname}Id)
        {
            return await m_{ModelName}RepositoryM.Get({modelname}Id);
        }
        public async Task<bool> Update({ModelName} {modelname})
        {
            return await m_{ModelName}RepositoryM.Update({modelname});
        }
        public async Task<bool> Delete(string id)
        {
            return await m_{ModelName}RepositoryM.Delete(id);
        }
        public async Task<List<{ModelName}>> Search(string searchStr)
        {
            searchStr = searchStr.ToLower();
            return await m_{ModelName}RepositoryM.Search(searchStr);
        }
    }
}
